name: payment-service — CI/CD

on:
  push:
    branches: ["main"]
    paths:
      - "backend/services/payment-service/**"
      - ".github/workflows/payment-service.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "backend/services/payment-service/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  security-events: write
  id-token: write

env:
  REGISTRY: ${{ secrets.AZURE_ACR_REGISTRY_URL_PAYMENT }}
  IMAGE_PREFIX: exotic-pet-marketplace
  SERVICE: payment-service

jobs:
  # Job 1: Install & Audit
  audit:
    name: Install & Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        working-directory: backend/services/payment-service
        run: npm install

      - name: npm audit
        working-directory: backend/services/payment-service
        run: npm audit --audit-level=high
        continue-on-error: true

  # Job 2: Snyk Security Scan
  security-scan:
    name: Snyk Security Scan
    runs-on: ubuntu-latest
    needs: audit
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        working-directory: backend/services/payment-service
        run: npm install

      - name: Run Snyk
        uses: snyk/actions/node@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --file=backend/services/payment-service/package.json --severity-threshold=high --sarif-file-output=snyk.sarif

      - name: Upload Snyk report
        uses: github/codeql-action/upload-sarif@v4
        continue-on-error: true
        if: always() && hashFiles('snyk.sarif') != ''
        with:
          sarif_file: snyk.sarif

  # Job 3: Build & Push to ACR
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: [audit, security-scan]
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Login to ACR
        run: |
          echo "${{ secrets.AZURE_ACR_PASSWORD_PAYMENT }}" | docker login -u "${{ secrets.AZURE_ACR_USERNAME_PAYMENT }}" --password-stdin "${{ env.REGISTRY }}"

      - name: Build and Push to ACR
        run: |
          docker build -t "${{ env.REGISTRY }}/exotic-pet-marketplace-payment-service:latest" \
                       -t "${{ env.REGISTRY }}/exotic-pet-marketplace-payment-service:${{ github.sha }}" \
                       backend/services/payment-service
          docker push "${{ env.REGISTRY }}/exotic-pet-marketplace-payment-service:latest"
          docker push "${{ env.REGISTRY }}/exotic-pet-marketplace-payment-service:${{ github.sha }}"

  # Job 4: Deploy to Azure Container Apps
  deploy:
    name: Deploy to Azure
    runs-on: ubuntu-latest
    needs: build-and-push
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          tenant-id: ${{ secrets.AZURE_TENANT_ID_PAYMENT }}
          client-id: ${{ secrets.AZURE_CLIENT_ID_PAYMENT }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_PAYMENT }}

      - name: Update Container App
        uses: azure/CLI@v2
        with:
          inlineScript: |
            az containerapp update \
              --name payment-service \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP_PAYMENT }} \
              --image ${{ env.REGISTRY }}/exotic-pet-marketplace-payment-service:latest \
              --set-env-vars "BUILD_ID=${{ github.run_id }}" "BUILD_SHA=${{ github.sha }}"
